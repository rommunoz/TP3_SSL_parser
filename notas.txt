primero ajustar sus rutas del launch y tasks si es que no lo hicieron ya
recordar que la carpeta .vscode por fuera de la carpeta de trabajo

comandos para generar y compilar:

bison -d parser.y
flex scanner.l
gcc -o parsetest main.c parser.c scanner.c -lfl

Ejecutar: en windows (en cmd, porque powershell no te permite usar >)
parsetest.exe <entrada.txt >salidaTest.txt

Aclaraciones y problemas:
-d es un argumento para que cree el parser.c y el parser.h
el orden de primero hacer el bison y desp el flex lo saque de un txt del profe
parsetest es solo un nombre arbitrario
-lfl: en linux anda bien, pero suele tirar error si no lo encuentra, entonces
reescribirlo así: -L«tu ruta» sin los '»'(ejemplo: -LC:\\msys64\\ucrt64\\bin\\lfl es mi ruta), -L Es para especificar la misma
el comando gcc tengan en cuenta que si sus fuentes se llaman distinto ajustenlos

Dudas: 
Hasta ahora es lo que pude hacer, esta gramática la traté de adaptar
para que no sea ambigua ni tenga recursividad a derecha por lo de insumo de memoria.
Mis mayores dudas estan en parser.y, y poco en scanner.l, pero corrijan lo que sepan y/o les parezca.